{
  "name": "orbit-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"Web app is ready in frontend/\"",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "cap:copy": "npx cap copy"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/android": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}